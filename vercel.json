{
  "version": 2,
  "build": {
    "env": {
      "NODEJS_HELPERS": "0"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}